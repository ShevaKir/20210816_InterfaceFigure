using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace _20210816_InterfaceFigure
{
    class Ellipse //: Сircle
    {
        //public int _radiusTwo;

        //public Ellipse(int x, int y, int radiusOne, int radiusTwo)
        //    : base(x, y, radiusOne)
        //{
        //    _radiusTwo = radiusTwo;

        //}

        //public override void Move(int deltaX, int deltaY)
        //{
        //    Coordinate.X += deltaX;
        //    Coordinate.Y += deltaY;
        //}

        //public override double GetArea()
        //{
        //    double area = 0;

        //    area = P * (Radius * _radiusTwo);

        //    return area;
        //}

        //public override double GetPerimetr()
        //{
        //    double perimetr = 0;

        //    perimetr = 4 * (P * Radius * _radiusTwo + (Radius - _radiusTwo)) 
        //            / (Radius + _radiusTwo);

        //    return perimetr;
        //}
    }
}